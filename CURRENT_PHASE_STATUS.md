# Текущий Этап Проекта

## Обновление от 2026-03-09
1. Собран новый детерминированный GT-пак для честной проверки ночного/IR трекинга:
   - `configs/regression_pack_ir_gt.csv`
   - Источник: `/Users/bround/Desktop/Обучение/train/*` (последовательности с `IR_label.json`).
2. Прогон baseline vs RTX-кандидат выполнен:
   - baseline (`preset=night`): `PASS`
   - candidate (`preset=night_rtx_candidate`, модель `rtx_drone_first_6h_v2_best.pt`): `FAIL`
3. Причина отклонения кандидата:
   - локальные регрессии по `active_id_changes_per_min` на ряде GT-клипов,
   - итог quality-gate не пройден, baseline не меняем.

## Этап
Стабилизация двухконтурной работы:
1. RTX-контур: надежный цикл обучения и резюма.
2. Mac-контур: улучшение трекинга и проверка качества моделей.

## Приоритет на текущий момент
1. Не срывать активные training-циклы из-за инфраструктуры.
2. Получать воспроизводимые артефакты каждого run.
3. Оценивать модель только через benchmark/quality-gate.

## Контрольные точки завершения этапа
1. Два последовательных run на RTX без дублирования контуров и аварий.
2. Принятие/отклонение модели по зафиксированному quality-gate.
3. Подтвержденный baseline для дальнейшей эволюции трекинга.

## Что делаем после завершения этапа
1. Переходим к следующему циклу улучшений логики сопровождения.
2. Готовим parity-перенос поведения на RPi5 + Hailo.
