# REPORT: TASK-20260312-046 — dependency-lock-and-bootstrap

Task ID: TASK-20260312-046
Date: 2026-03-12
Status: Done

## Что сделано

1. Создан `requirements.txt` — минимальный production runtime dependency contract:
   - Core runtime: `torch>=2.0`, `torchvision>=0.15`, `ultralytics>=8.0`, `opencv-python>=4.8`, `numpy>=1.24`, `PyYAML>=6.0`, `lapx>=0.5`, `scipy>=1.10`, `tqdm>=4.65`, `pillow>=10.0`
   - GUI: `PySide6>=6.5`
   - Evaluation/intake: `requests>=2.28`, `matplotlib>=3.7`
   - Версии заданы как `>=` минимальные, без жёсткого pinning
   - Явная пометка: training-specific deps (wandb и др.) — не обязательны для desktop runtime

2. Обновлён `RUNBOOK.md`:
   - Секция "Требования к окружению" заменена на "Bootstrap (первый запуск / новая машина)"
   - Добавлены полные шаги: `python3.11 -m venv` → `pip install -r requirements.txt` → baseline copy → smoke check
   - Источник: сканирование import footprint `src/`, `app/`, `python_scripts/` + `pip list` в `tracker_env`

## Что проверено

- `python3 -m compileall -q python_scripts src app orchestrator tests` — чисто.
- `PYTHONPATH=src ./tracker_env/bin/python main_tracker.py --help` — работает.

## Риски

- `requirements.txt` зафиксирует минимальные версии. Жёсткого lockfile нет — если нужен точный reproducible build, потребуется `pip freeze > requirements-lock.txt` отдельно.

## Что осталось

- Ничего. TASK закрыт.
