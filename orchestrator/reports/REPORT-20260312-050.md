# REPORT: TASK-20260312-050 — accepted-model-install-flow

Task ID: TASK-20260312-050
Date: 2026-03-12
Status: Done

## Что сделано

1. Создан `python_scripts/install_baseline.py` — minimal install helper:
   - `--source <path>` — путь к принятому .pt (required)
   - `--notes <str>` — описание, дата, reason (optional)
   - `--gate-report <path>` — путь к gate JSON для traceability (optional)
   - `--dry-run` — предварительный просмотр без копирования
   - Действия: sha256 источника → copy → `models/baseline.pt` → write `models/baseline_manifest.json`
   - Manifest fields: `installed_at`, `source_path`, `source_sha256`, `notes`, `gate_report_path`

2. Обновлён `RUNBOOK.md` секция "Как обновить baseline":
   - Заменён `cp` на полный install flow с `install_baseline.py`
   - Добавлены пояснения: только манифест попадает в git, `.pt` не хранится

## Что проверено

- `PYTHONPATH=src ./tracker_env/bin/python python_scripts/install_baseline.py --help` — работает.
- `python3 -m compileall -q` — чисто.

## Риски

- `install_baseline.py` не проверяет, что source является корректным YOLO .pt — scope намеренно минимальный.

## Что осталось

- Ничего. TASK закрыт.
