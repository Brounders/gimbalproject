# REPORT: TASK-20260311-004-select-active-policy-tests

## Summary
Расширен тестовый набор `tests/test_target_manager_lock_policy.py`: добавлен класс `TestSelectActivePolicy` с 4 детерминированными тестами `select_active()`. Покрыты все ключевые ветки метода. Runtime-код не изменялся.

Добавленные сценарии:
1. `test_select_active_keeps_active_primary` — активная primary-цель удерживается при появлении более сильного конкурента (Phase-1 early return).
2. `test_select_active_switches_from_aux_to_primary` — aux/night цель заменяется на primary (Phase-3, drone_score + speed > 0.5).
3. `test_select_active_activates_aux_when_no_primary` — aux-цель с conf ≥ 0.30 активируется при отсутствии primary (Phase-4).
4. `test_select_active_rejects_weak_aux` — aux с conf=0.25 и speed=1.0 отклоняется (Phase-4 gate).

## Changes
- File: `tests/test_target_manager_lock_policy.py` — добавлен класс `TestSelectActivePolicy` (4 теста, строки 230–295)

## Validation
- Command: `PYTHONPATH=src ./tracker_env/bin/python -m unittest -v tests.test_target_manager_lock_policy`
- Result: Ran 12 tests — OK (8 старых + 4 новых, всё зелёное)
- Command: `PYTHONPATH=src python3 -m compileall -q tests src`
- Result: OK

Полный прогон обоих файлов: `Ran 15 tests in 0.001s — OK`.

## Residual Risks
- Risk: тест `test_select_active_switches_from_aux_to_primary` использует `mgr.targets[2].speed = 0.8` напрямую. Если `TrackedTarget` изменит имя поля `speed`, тест потребует обновления.
- Mitigation: проверяется контракт (`active_id`), а не приватное состояние.

## Next Step
- Proposed follow-up: добавить тест на конкуренцию двух primary-целей с различным `drone_score` (проверить, что лучший score побеждает при свободном switch).
