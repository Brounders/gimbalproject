# REPORT: TASK-20260312-072 — problem-pack mini-gate enforcement

Task ID: TASK-20260312-072
Date: 2026-03-12
Status: Done

## Что сделано

1. Создан `configs/regression_pack_problem_night.csv`:
   - 2 клипа: `night_ground_large_drones.mp4` (night) + `night_ground_indicator_lights.mp4` (noise)
   - Использовать с `--preset night`

2. Создан `configs/regression_pack_problem_ir.csv`:
   - 2 клипа: `IR_DRONE_001.mp4` (ir) + `Demo_IR_DRONE_146.mp4` (ir)
   - Использовать с `--preset antiuav_thermal`

3. Обновлён `configs/problem_pack_gate_contract.json`:
   - Добавлено: `"night_pack_file": "configs/regression_pack_problem_night.csv"`
   - Добавлено: `"ir_pack_file": "configs/regression_pack_problem_ir.csv"`

4. Создан `python_scripts/run_problem_pack_gate.py`:
   - Читает `configs/problem_pack_gate_contract.json`
   - Запускает `run_quality_gate.py` дважды: night preset на ночных клипах, ir preset на IR клипах
   - Exit code: 0 (all pass) или 4 (any fail), 2 (error — contract/pack не найден)

5. Обновлён `RUNBOOK.md`:
   - Секция "Problem-pack mini-gate" дополнена one-command invocation:
     `PYTHONPATH=src python python_scripts/run_problem_pack_gate.py`
   - Ручные invocations обновлены на split CSV файлы и `--max-noise-false-lock-rate 0.35`

## Что проверено

- `python3 -m compileall -q python_scripts/run_problem_pack_gate.py` — OK
- `python3 -m compileall -q python_scripts src app orchestrator tests` — OK
- 15 unit tests — OK

## Риски

- Нет: `run_problem_pack_gate.py` — только subprocess wrapper, не содержит gate логики.
- CSV файлы содержат пути к test_videos — если clip отсутствует, `run_quality_gate.py`
  вернёт ошибку (штатно обрабатывается).
- `night_ground_indicator_lights.mp4` требует наличия файла в test_videos/ — не включён
  в git (слишком большой), но присутствует в dev environment.

## Что осталось

- Ничего. TASK закрыт.
