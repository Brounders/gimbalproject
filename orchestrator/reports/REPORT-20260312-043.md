# REPORT: TASK-20260312-043 — theme-source-resolution

Task ID: TASK-20260312-043
Date: 2026-03-12
Status: Done

## Что сделано

1. Создан `app/ui/theme.py` — канонический source of truth для `APP_STYLESHEET`.
   - Файл содержит module docstring и полную строку `APP_STYLESHEET` (224 строки Qt CSS).
   - Полностью идентичен inline-блоку, который ранее жил в `app/main_gui.py`.

2. Обновлён `app/main_gui.py`:
   - Удалён inline-блок `APP_STYLESHEET = """..."""` (строки 49–272 до правки).
   - Добавлен импорт: `from app.ui.theme import APP_STYLESHEET`.
   - `self.setStyleSheet(APP_STYLESHEET)` продолжает работать без изменений.

## Что проверено

- `python3 -m compileall -q python_scripts src app orchestrator tests` — чисто, без ошибок.
- GUI smoke: `QT_QPA_PLATFORM=offscreen MainWindow()` → `MainWindow` — без crash.
- Визуальный baseline не изменён: CSS-строка перемещена без правок.

## Риски

- Нет: изменение строго структурное (перенос строки). Содержимое `APP_STYLESHEET` не изменялось.

## Что осталось

- Ничего. TASK закрыт.
