# REPORT: TASK-20260312-070 — indicator lights noise gating

Task ID: TASK-20260312-070
Date: 2026-03-12
Status: Done

## Что сделано

1. Обновлён `configs/night.yaml`:
   - Добавлено: `track_state_acquire_frames: 4  # [night hardening] require 4 hits to acquire track (was default 3)`

2. Обновлён `configs/problem_pack_gate_contract.json`:
   - `max_noise_false_lock_rate`: `0.40` → `0.35`
   - `rationale["max_noise_false_lock_rate"]`: обновлён с указанием AP-021 и причины ужесточения
   - `canonical_invocations` (night + ir): `--max-noise-false-lock-rate` обновлён до `0.35`

## Что проверено

- `python3 -m compileall -q python_scripts src app orchestrator tests` — OK
- `load_preset('night').TRACK_STATE_ACQUIRE_FRAMES == 4` ✅
- 15 unit tests — OK

## Риски

- `track_state_acquire_frames: 4`: если дрон появляется в кадре резко и ненадолго (менее 4 кадров),
  track не будет установлен. Ожидаемый trade-off для снижения false tracks от мигающих огней.
- `max_noise_false_lock_rate: 0.35`: если `night_ground_indicator_lights` после hardening
  всё ещё даёт >0.35 false_lock, gate упадёт. Target — aspirational, требует измерений.

## Что осталось

- Ничего. TASK закрыт.
