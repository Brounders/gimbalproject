# REPORT: TASK-20260312-052 — pipeline-frameoutput-extract

Task ID: TASK-20260312-052
Date: 2026-03-12
Status: Done

## Что сделано

1. Создан `src/uav_tracker/frame_result.py`:
   - Module docstring с пояснением источника
   - Imports: `from __future__ import annotations`, `dataclass`, `Optional`, `numpy`
   - Полное определение `class FrameOutput` (29 полей) — точная копия из pipeline.py

2. Обновлён `src/uav_tracker/pipeline.py`:
   - Удалён `from dataclasses import dataclass` из imports (больше не нужен)
   - Блок `@dataclass\nclass FrameOutput:...` (30 строк) заменён на:
     `from uav_tracker.frame_result import FrameOutput`

3. Scan external consumers: FrameOutput используется только внутри pipeline.py (lines 746, 903). Re-export не потребовался.

## Что проверено

- `python3 -m compileall -q python_scripts src app orchestrator tests` — OK.
- `PYTHONPATH=src python -m unittest -q tests.test_target_manager_lock_policy tests.test_package_import` — 15 тестов OK.
- `PYTHONPATH=src ./tracker_env/bin/python main_tracker.py --help` — OK.
- GUI smoke: `MainWindow` — OK.

## Риски

- Нет: FrameOutput перемещён без изменений. Внешних потребителей нет.

## Что осталось

- Ничего. TASK закрыт.
