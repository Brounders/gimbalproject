# REPORT: TASK-20260312-053 — pipeline-overlay-extract

Task ID: TASK-20260312-053
Date: 2026-03-12
Status: Done

## Что сделано

1. Создан `src/uav_tracker/overlay.py`:
   - Module docstring с пояснением источника и назначения
   - Imports: `cv2`, `numpy`, `Optional`, `Config`, `TargetManager`, `TrackedTarget`
   - Перенесены 4 draw-функции из pipeline.py:
     - `_draw_target` (рисует bounding box + label + trail)
     - `_target_class_label` (текстовый label для класса)
     - `_draw_active_reticle` (crosshair reticle overlay)
     - `draw_frame` (главная HUD-функция, ~145 строк)

2. Обновлён `src/uav_tracker/pipeline.py`:
   - Весь блок из 4 функций (lines 1003–1214, ~212 строк) заменён на:
     `from uav_tracker.overlay import _draw_active_reticle, _draw_target, _target_class_label, draw_frame`
   - `run_tracker` продолжает вызывать `draw_frame` — теперь через этот import

3. Scan external consumers: `draw_frame`, `_draw_target` и прочие используются только в pipeline.py. Re-export не потребовался.

## Что проверено

- `python3 -m compileall -q python_scripts src app orchestrator tests` — OK.
- `PYTHONPATH=src python -m unittest -q tests.test_target_manager_lock_policy tests.test_package_import` — 15 тестов OK.
- `PYTHONPATH=src ./tracker_env/bin/python main_tracker.py --help` — OK.
- GUI smoke: `MainWindow` — OK.

## Риски

- Нет: 4 функции перемещены без изменений сигнатур и логики.

## Что осталось

- Ничего. TASK закрыт.
