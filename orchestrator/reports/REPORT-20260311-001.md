# REPORT: TASK-20260311-001-pipeline-overlay-extract-stage1

## Summary
Выполнен перенос overlay-функций из `src/uav_tracker/pipeline.py` в новый модуль `src/uav_tracker/overlay.py`. Сигнатуры функций, логика и комментарии сохранены без изменений. Публичный API `pipeline.py` (run_tracker, TrackerPipeline, apply_runtime_preset) не изменился.

Перенесено 4 функции (~252 строки):
- `_draw_target()`
- `_target_class_label()`
- `_draw_active_lock_box()`
- `draw_frame()`

## Changes
- File: `src/uav_tracker/overlay.py` — создан, содержит 4 overlay-функции
- File: `src/uav_tracker/pipeline.py` — удалены 4 функции, добавлен `from uav_tracker.overlay import draw_frame`

Размер pipeline.py: 1223 → 970 строк (−253).

## Validation
- Command: `PYTHONPATH=src python3 -m compileall -q src app`
- Result: OK, ошибок нет
- Command: `PYTHONPATH=src python3 -c "from uav_tracker.pipeline import run_tracker, TrackerPipeline, apply_runtime_preset; print('OK')"`
- Result: OK
- Command: `PYTHONPATH=src python3 -c "from uav_tracker.overlay import draw_frame; print('OK')"`
- Result: OK

Smoke-прогон на тестовом видео не выполнен (отсутствует `test_videos/night_ground_large_drones.mp4`). Компиляция и import chain полностью чистые.

## Residual Risks
- Risk: если внешний код импортирует `draw_frame` напрямую из `pipeline`, получит ImportError. Поиск показал — такого импорта нет.
- Risk: smoke-прогон на реальном видео не выполнен из-за отсутствия файла. Рекомендуется прогнать на любом доступном тестовом видео перед слиянием.

## Next Step
- Proposed follow-up: вынести `VideoSession` + `run_tracker` в отдельный `runner.py` как следующий этап снижения монолитности pipeline.py.
