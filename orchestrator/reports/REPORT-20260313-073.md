# REPORT: TASK-20260313-073 — large target night policy fix

Task ID: TASK-20260313-073
Date: 2026-03-13
Status: Done

## Что сделано

1. Обновлён `configs/night.yaml`:
   - Добавлено: `track_state_lost_frames: 12  # [night hardening] hold track 12 frames before LOST (was default 8)`
   - Добавлено: `lock_tracker_min_score: 0.52  # [night hardening] stricter correlation gate (was default 0.42)`

2. Обновлён `OPERATOR_BASELINE.md`:
   - Добавлена секция "Runtime Hardening Changes (AP-022)" с таблицей 2 изменений
   - Таблица "Preset Runtime Tuning Contract" дополнена строками:
     `track_state_lost_frames`, `lock_tracker_min_score`

## Что проверено

- `python3 -m compileall -q python_scripts src app orchestrator tests` — OK
- `load_preset('night').TRACK_STATE_LOST_FRAMES == 12` ✅
- `load_preset('night').LOCK_TRACKER_MIN_SCORE == 0.52` ✅
- 15 unit tests — OK

## Риски

- `track_state_lost_frames: 12`: tracker держит потерянный трек 12 кадров (вместо 8).
  Если дрон реально ушёл из кадра, tracker дольше "висит" без детекции.
  Ожидаемый trade-off: меньше ложных бросков при кратковременных пропаданиях large drone.
- `lock_tracker_min_score: 0.52`: если large drone делает резкий манёвр, correlation score
  может упасть ниже 0.52 — lock tracker потеряет цель. Это ожидаемое поведение
  (лучше потерять и реакварировать, чем держать false lock).

## Что осталось

- Ничего. TASK закрыт.
