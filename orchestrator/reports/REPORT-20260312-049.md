# REPORT: TASK-20260312-049 — baseline-governance-contract

Task ID: TASK-20260312-049
Date: 2026-03-12
Status: Done

## Что сделано

1. Создан `models/README.md` — канонический локальный governance contract:
   - Таблица четырёх model states: `baseline`, `candidate`, `hold_and_tune`, `reject`
   - Local artifacts для каждого state (path, manifest)
   - State transition rules: quality gate PASS → baseline install, FAIL → hold_and_tune или reject
   - Ссылки на governance thresholds (`run_quality_gate.py --help`) и operator tolerance (`OPERATOR_BASELINE.md`)
   - Install flow с примером команды `install_baseline.py`

2. Обновлён `RUNBOOK.md` секция "Baseline model → Как обновить baseline":
   - Добавлена ссылка `Governance contract: models/README.md`
   - Инструкция по install теперь указывает на `install_baseline.py` (задача TASK-050)

## Что проверено

- `python3 -m compileall -q python_scripts src app orchestrator tests` — чисто.

## Риски

- Нет: только документация.

## Что осталось

- Ничего. TASK закрыт.
