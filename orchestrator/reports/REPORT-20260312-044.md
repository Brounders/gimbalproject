# REPORT: TASK-20260312-044 — local-model-baseline-freeze

Task ID: TASK-20260312-044
Date: 2026-03-12
Status: Done

## Что сделано

1. Обновлены все 4 operator preset-конфига — `model_path` переведён на стабильный локальный path:
   - `configs/default.yaml`: `runs/detect/.../best.pt` → `models/baseline.pt`
   - `configs/night.yaml`: то же самое
   - `configs/small_target.yaml`: то же самое
   - `configs/antiuav_thermal.yaml`: то же самое
   - `configs/rpi_hailo.yaml` — не тронут (уже корректен: `models/drone_bird.hef`)

2. Добавлена секция "Baseline model" в `RUNBOOK.md`:
   - Определён canonical production path: `models/baseline.pt`
   - Задокументирован процесс обновления baseline (copy accepted .pt → `models/baseline.pt`)
   - Добавлено предупреждение о ручном шаге при первом развёртывании

## Что проверено

- `rg -n "model_path:" configs` — все 4 YAML пресета указывают на `models/baseline.pt`.
- `python3 main_tracker.py --help` — CLI работает корректно.
- `python3 -m compileall -q` — без ошибок.

## Риски

- `models/baseline.pt` физически не существует в репозитории. Это намеренно: задача фиксирует CONTRACT, а не создаёт модель. При первом запуске оператор должен скопировать принятую модель в `models/baseline.pt` согласно RUNBOOK.

## Что осталось

- Ничего. TASK закрыт.
