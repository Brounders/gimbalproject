# REPORT: TASK-20260312-058 — preset-gate-enforcement

Task ID: TASK-20260312-058
Date: 2026-03-12
Status: Done

## Что сделано

1. Обновлён `python_scripts/run_quality_gate.py`:
   - Добавлен `--context {day,night,ir}` аргумент в `parse_args()`
   - Добавлен `_CONTEXT_MAP` dict: `day` → `(default, regression_pack_day.csv)`, `night` → `(night, regression_pack_night.csv)`, `ir` → `(antiuav_thermal, regression_pack_ir.csv)`
   - В начале `main()` добавлена логика: если `args.context` задан, перезаписывает `args.preset` и `args.pack_file`
   - В JSON summary добавлены поля: `report_type: "quality_gate"`, `context: args.context`
   - Существующие вызовы без `--context` не затронуты (default="")

2. Обновлён `RUNBOOK.md`:
   - Исправлен exit code: `1` → `4` (FAIL)
   - Добавлена подсекция "Канонический запуск по контексту" с тремя `--context` invocations
   - Добавлена инструкция candidate + `--baseline` comparison

## Что проверено

- `python3 -m compileall -q python_scripts` — OK
- `run_quality_gate.py --help` — `--context {day,night,ir}` присутствует
- 15 unit tests — OK

## Риски

- Нет: `--context` — optional, не ломает существующие invocations. `_CONTEXT_MAP` — чистый dict без side effects.

## Что осталось

- Ничего. TASK закрыт.
