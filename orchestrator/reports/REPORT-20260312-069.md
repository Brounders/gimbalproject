# REPORT: TASK-20260312-069 — problem-pack threshold contract

Task ID: TASK-20260312-069
Date: 2026-03-12
Status: Done

## Что сделано

1. Создан `configs/problem_pack_gate_contract.json`:
   - Threshold contract для problem pack clips (более lenient, чем основной gate)
   - Поля: description, note, night/ir presets, thresholds dict, rationale dict,
     canonical_invocations (night + ir)
   - Включает rationale для каждого threshold (почему lenient)

2. Обновлён `RUNBOOK.md`:
   - В секцию Evaluation добавлена подсекция "Problem-pack mini-gate (threshold contract)"
   - Canonical invocations для night и ir пресетов с явными threshold overrides
   - Ссылка на `configs/problem_pack_gate_contract.json`

## Что проверено

- `configs/problem_pack_gate_contract.json` создан и содержит все поля
- `RUNBOOK.md` содержит "Problem-pack mini-gate" секцию
- `python3 -m compileall -q` — OK (JSON не Python, compile ok для всего остального)
- 15 unit tests — OK

## Риски

- Нет: только JSON config + RUNBOOK documentation. Никакого runtime кода.
- Thresholds выбраны как aspirational targets после AP-018..020 hardening;
  требуют обновления по результатам измерений на реальных клипах.

## Что осталось

- Ничего. TASK закрыт.
