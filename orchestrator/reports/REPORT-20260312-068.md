# REPORT: TASK-20260312-068 — large-drones night stability fix

Task ID: TASK-20260312-068
Date: 2026-03-12
Status: Done

## Что сделано

1. Обновлён `configs/night.yaml`:
   - Добавлено: `active_id_switch_cooldown_frames: 60  # [night hardening] double cooldown (was default 30)`
   - Изменено: `class_ema_alpha: 0.22` → `class_ema_alpha: 0.15  # [night hardening] slower EMA (was 0.22)`

2. Обновлён `OPERATOR_BASELINE.md`:
   - Добавлена секция "Runtime Hardening Changes (AP-020)" с таблицей 2 изменений
   - Таблица "Preset Runtime Tuning Contract" дополнена строками:
     `active_id_switch_cooldown_frames` и `class_ema_alpha`

## Что проверено

- `python3 -m compileall -q python_scripts src` — OK
- `load_preset('night').ACTIVE_ID_SWITCH_COOLDOWN_FRAMES == 60` ✅
- `load_preset('night').CLASS_EMA_ALPHA == 0.15` ✅
- 15 unit tests — OK

## Риски

- `active_id_switch_cooldown_frames: 60`: снижает частоту ID-switches. Если цель быстро
  покидает кадр и возвращается, новый ID может быть не подхвачен в течение 60 кадров.
  Это ожидаемый trade-off для noise/instability reduction.
- `class_ema_alpha: 0.15` (ниже default 0.18): drone_score обновляется медленнее.
  Может незначительно увеличить задержку lock на быстро появляющихся целях.

## Что осталось

- Ничего. TASK закрыт.
