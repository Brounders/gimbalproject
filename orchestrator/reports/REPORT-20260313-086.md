# REPORT: TASK-20260313-086 — Model Branch Decision

Task ID: TASK-20260313-086
Date: 2026-03-13
Status: Done
Type: Decision artifact. No code changes. decision_log.json updated.

---

## Decision

**`reject_and_reset_training_strategy`**

### Branch

- Dataset: `drone-bird-yolo`
- Run name: `curriculum_drone-bird-yolo`
- Epochs completed: 132 of 144
- Artifacts published: chunk1_v2 through chunk10

### Reason

All tested candidates (chunk6 ep73-84, chunk10 ep121-132) fail the night quality gate
with massive regression vs de-facto baseline:

| Metric | De-facto Baseline | Chunk6 | Chunk10 |
|--------|------------------|--------|---------|
| Night large_drones false_lock | **0.510** | 0.830 | 0.951 |
| Night large_drones id_chg/min | **12.23** | 48.93 | 55.05 |
| Night indicator false_lock | **0.096** | 0.973 | 0.902 |
| Night gate | **PASS** | FAIL | FAIL |

The trend shows **worsening with more epochs** — this is not a tuning range issue; the model
is diverging from night-usable representations as the curriculum progresses. No amount of
further epoch budget on this curriculum will produce a promotable candidate.

### No baseline installation performed

The de-facto baseline (`runs/detect/runs/drone_bird_probe_fast/weights/best.pt`) is superior
on night and is NOT being replaced. However, it also requires formal baseline installation
before any training comparison cycle. That is a separate governance task.

---

## Recommended next steps (not activating without orchestrator approval)

1. **Analyze training dataset composition**: `drone-bird-yolo` with `scene_profile=ir` may be
   training primarily on thermal imagery, causing night visible-light regression. Need to audit
   the actual dataset distribution.

2. **Baseline formalization**: install `drone_bird_probe_fast` as formal `models/baseline.pt`
   with `install_baseline.py` after running all three context gates against it with
   current accepted thresholds.

3. **Strategy reset options** (for orchestrator review):
   a. Re-train from scratch with a dataset that includes both night visible-light AND IR clips
   b. Fine-tune `drone_bird_probe_fast` specifically on the problematic cases
   c. Investigate if `drone-bird-yolo` dataset contains visible-light night data at all

---

## Traceability

Gate reports:
- Day (chunk10): `runs/evaluations/quality_gate/ap026_chunk10_dayonlyquality_gate_default.json`
- Night (chunk6): `runs/evaluations/quality_gate/ap026_chunk6_nightquality_gate_night.json`
- Night (chunk10): `runs/evaluations/quality_gate/ap026_chunk10_nightquality_gate_night.json`
- IR (chunk10): `runs/evaluations/quality_gate/ap026_chunk10_irquality_gate_antiuav_thermal.json`
- Baseline day: `runs/evaluations/quality_gate/ap026_baseline_dayquality_gate_default.json`
- De-facto baseline night: recorded in AP-025 (tag `ap025_confirm5`)
