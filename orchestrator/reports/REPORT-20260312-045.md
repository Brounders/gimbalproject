# REPORT: TASK-20260312-045 — legacy-root-cleanup-and-launch-contract

Task ID: TASK-20260312-045
Date: 2026-03-12
Status: Done

## Что сделано

1. Создана директория `legacy/`.

2. Перемещены legacy-скрипты из корня репозитория в `legacy/` через `git mv`:
   - `benchmark.py` → `legacy/benchmark.py` (M1 matmul + YOLO perf benchmark)
   - `real_tracker.py` → `legacy/real_tracker.py` (ранний прототип camera tracker)
   - `train_script.py` → `legacy/train_script.py` (ранний training harness)

3. Создан `legacy/README.md`:
   - Описание каждого файла с пояснением его статуса
   - Ссылка на canonical entrypoints (`tracker_gui.py`, `main_tracker.py`)

4. Обновлён `RUNBOOK.md`:
   - Секция "Non-canonical скрипты (legacy)" переработана: теперь указывает на `legacy/` с таблицей файлов
   - Добавлена ссылка на `legacy/README.md`

5. Canonical entrypoints НЕ тронуты:
   - `main_tracker.py` — остаётся в корне
   - `tracker_gui.py` — остаётся в корне

## Что проверено

- `python3 main_tracker.py --help` — работает корректно.
- `python3 -m compileall -q python_scripts src app orchestrator tests` — без ошибок.
- `ls legacy/` — содержит `benchmark.py`, `real_tracker.py`, `train_script.py`, `README.md`.
- `ls benchmark.py real_tracker.py train_script.py 2>&1` — файлы отсутствуют в корне.

## Риски

- Нет: `git mv` сохраняет историю изменений для всех перемещённых файлов.

## Что осталось

- Ничего. TASK закрыт.
