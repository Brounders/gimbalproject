# REPORT: TASK-20260313-081 — Large-Target Continuity Gating

Task ID: TASK-20260313-081
Date: 2026-03-13
Status: Done
Type: Code (mapping) + YAML + Documentation. No training, no GUI, no runtime-wide rewrite.

---

## Что сделано

### 1. `src/uav_tracker/profile_io.py` — добавлены два mapping entry

После `'night_lost_max': 'NIGHT_LOST_MAX'`:
```python
'night_confirm': 'NIGHT_CONFIRM',
'night_max_ar': 'NIGHT_MAX_AR',
```

`NIGHT_CONFIRM` (default=3): счётчик последовательных кадров в `night_detector.py:74`.
Кандидат добавляется в детекции только если присутствует >= NIGHT_CONFIRM кадров подряд.
Увеличение защищает существующий large-drone track от transient noise replacements.

`NIGHT_MAX_AR` (default=3.0): фильтр aspect ratio для night blobs. Экспонирован для
возможного будущего tuning.

### 2. `configs/night.yaml` — добавлен continuity gating knob

```yaml
# [night detector — continuity gating] AP-025
night_confirm: 4         # default 3: require 4 consecutive frames to establish night detection
```

### 3. `OPERATOR_BASELINE.md` — три правки

- Исправлены значения AP-024 в таблице: 500/80/16 → 220/65/8 (фактические финальные значения)
- Восстановлен lock_lost_grace: 1→2 в таблице (изменение из TASK-082)
- Расширена секция Night Detector Knobs: добавлены night_confirm и night_max_ar
- Добавлены фактические AP-024 значения в описание секции

---

## Что проверено

- `python3 -m compileall -q python_scripts src app` — **PASS**
- pytest — **No pytest suite configured** (ожидаемо)

---

## Контракт после правки

| YAML ключ | Config поле | Default | Night пресет | Готов к тюнингу |
|-----------|------------|---------|-------------|----------------|
| `night_confirm` | `NIGHT_CONFIRM` | 3 | **4** | ✅ |
| `night_max_ar` | `NIGHT_MAX_AR` | 3.0 | не задан (default) | ✅ |

---

## Риски

- night_confirm=4 требует на 1 кадр больше для establishing нового night target.
  Для быстрого large drone это может означать 1 кадр задержки при первичном detection.
- Риск минимален: lock policy уже имеет cooldown (60 кадров), поэтому 1 доп. кадр
  на confirmation не влияет на lock stability.

---

## Что осталось

- TASK-082: lock_lost_grace tuning + gate evidence
- TASK-083: hard gate evidence + final verdict
