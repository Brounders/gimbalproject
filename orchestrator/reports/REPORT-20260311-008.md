# REPORT: TASK-20260311-008-filesystem-classification

## Summary
Создана матрица классификации файловой системы проекта по архитектурным ролям и критичности.
Источник данных: REPORT-20260311-007 (инвентаризация). Артефакт: `orchestrator/state/filesystem_classification.md`.

## Changes
| Файл | Действие |
|---|---|
| `orchestrator/state/filesystem_classification.md` | Создан — матрица классификации |

## Ключевые выводы

### Классы и риски
| Класс | Описание | Риск |
|---|---|---|
| `core` | `src/`, `app/`, `main_tracker.py`, `tracker_gui.py`, `configs/` | HIGH |
| `training-tools` | `python_scripts/`, curriculum/regression configs | MEDIUM |
| `datasets-artifacts` | `datasets/`, `runs/`, `models/`, `imports/` | HIGH (объём 23.6 GB) |
| `observability` | `orchestrator/`, `logs/`, `test_videos/` | LOW |
| `legacy` | `legacy/prototypes/`, `changesets/`, `safety_snapshots/` | LOW |
| `candidate-cleanup` | 5 root-wrapper .py + `arduino_sketches/` | LOW |

### Ключевое наблюдение: root-level wrappers
Файлы `benchmark.py`, `real_tracker.py`, `tracker_final.py`, `HYBRID_NIGHT_TRACKER.py`,
`train_script.py` в корне — это **compatibility wrappers** (`"""Compatibility wrapper for archived prototype script."""`).
Реальные прото-скрипты уже перенесены в `legacy/prototypes/`. Обёртки безопасно убрать в Phase 2.

### Все критические entrypoints присутствуют
Проверено 11 entrypoints: **все YES**. Runtime не затронут.

## Validation
- Ссылки на пути и размеры верифицированы из REPORT-20260311-007.
- Все runtime entrypoints (`main_tracker.py`, `tracker_gui.py`, `app/*`, `src/*`) отнесены к `core` — non-removable.
- compileall: OK.

## Residual Risks
- `datasets/antiuav_rgbt_ir_yolo/images/val` занимает 10.3 GB — основная часть датасета. Не дублируется.
- `imports/rtx/unpacked/` содержит старые unpacked RTX артефакты (100+ MB). Можно архивировать после verification в Phase 2.

## Следующий шаг
- TASK-009: безопасный план реорганизации на основе этой классификации.
