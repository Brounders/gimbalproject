# REPORT: TASK-20260311-013

Task ID: TASK-20260311-013
Owner: Claude Mac
Status: Completed
Date: 2026-03-11

## Что сделано

Создан `RUNBOOK.md` — единый источник истины по запуску и ролям команд.
Добавлена ссылка в `PROJECT_ARCHITECTURE.md`.

### RUNBOOK.md охватывает

- Canonical entrypoints: `tracker_gui.py` (GUI) и `main_tracker.py` (CLI) как тонкие прокси
- Canonical operator modes: Авто / День / Ночь / IR → preset mapping
- Evaluation: `run_quick_kpi_smoke.py`, `run_quality_gate.py`, `run_offline_benchmark.py`, `run_scenario_sweep.py`
- Training: `train_yolo_from_yaml.py`, `train_drone_bird.py` + утилиты датасета
- Orchestration: `check_orchestration_state.py`, directories
- Tests: команда `python -m unittest`
- Presets: таблица `configs/*.yaml`
- Non-canonical (legacy) скрипты с явной пометкой
- Требования к окружению и smoke-импорт

### PROJECT_ARCHITECTURE.md

Добавлена строка `> Для канонических команд запуска см. **RUNBOOK.md**.` в начало файла.

## Какие файлы изменены/созданы

- `RUNBOOK.md` (new)
- `PROJECT_ARCHITECTURE.md` (+1 строка ссылки)

## Результаты валидации

```
python3 -m compileall -q python_scripts src app orchestrator → OK
```
Команды из runbook верифицированы: `tracker_gui.py`, `main_tracker.py`, `run_quick_kpi_smoke.py --help`,
`check_orchestration_state.py` — все файлы существуют.

## Риски

- Нет. Документация не влияет на runtime.
