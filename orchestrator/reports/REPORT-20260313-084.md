# REPORT: TASK-20260313-084 — Local Baseline Operational Check

Task ID: TASK-20260313-084
Date: 2026-03-13
Status: Done
Type: Analysis-only. No code changes.

---

## Findings

### 1. Baseline artifacts

| Artifact | Path | State |
|----------|------|-------|
| `models/baseline.pt` | `models/baseline.pt` | **ABSENT** |
| `models/baseline_manifest.json` | `models/baseline_manifest.json` | **ABSENT** |

No formal baseline installation has ever been performed via `install_baseline.py`.

### 2. Runtime fallback in effect

`src/uav_tracker/pipeline.py:114` implements `resolve_model_path()` which tries a list of candidates in order:
```
models/baseline.pt             ← absent → skip
runs/detect/runs/drone_bird_probe_fast/weights/best.pt  ← EXISTS → used
runs/detect/runs/drone_bird_v2/weights/best.pt
runs/detect/runs/drone_bird_v1/weights/best.pt
models/yolo11n.pt
yolo11n.pt
```

**All quality gate runs since the start of the project (AP-018 through AP-025) have used
`runs/detect/runs/drone_bird_probe_fast/weights/best.pt` as the de-facto model.**

This file exists and the runtime loads it correctly. However, it has no traceability manifest,
no sha256 record, and no formal acceptance gate tied to it.

### 3. install_baseline.py

`install_baseline.py --help` responds correctly — the CLI is operational. The install flow
is documented in `models/README.md` and is ready to use. It has simply never been invoked.

### 4. Local checkpoints in models/checkpoints/

The `models/checkpoints/` directory contains previously-evaluated RTX candidates:
- `rtx_drone_first_6h_v2_best.pt` — rejected (prior cycle)
- `rtx_drone_stability_12h_v1_epoch176_best.pt` — rejected (REPORT-031)
- `rtx_latest_best.pt` — alias/symlink, no explicit acceptance decision

None of these are installed as baseline.

### 5. decision_log.json

`automation/state/decision_log.json`: `"decisions": []` — empty. No formal model decision has
ever been recorded.

---

## Assessment

| Check | Result |
|-------|--------|
| `models/baseline.pt` exists | ❌ ABSENT |
| `models/baseline_manifest.json` exists | ❌ ABSENT |
| Runtime model loads correctly | ✅ YES (via fallback to `drone_bird_probe_fast`) |
| `install_baseline.py` CLI operational | ✅ YES |
| Any formal baseline installation performed | ❌ NEVER |
| Any formal model decision recorded | ❌ NEVER |

**Conclusion:** The local baseline contract is **NOT formally operational**. The runtime
functions via an implicit fallback to a legacy `drone_bird_probe_fast` model. The install
infrastructure (`install_baseline.py`, `models/README.md`, `decision_log.json`) is in place
and ready to be used, but has never been invoked.

This is the primary motivation for AP-026: to formalize the current model state and make
an explicit branch decision on the curriculum candidates.

---

## Recommended next step (TASK-085)

Evaluate the published curriculum candidates (`chunk10`, `chunk6`) under the current accepted
runtime (AP-025 state: night_confirm=5) and compare against the `drone_bird_probe_fast` de-facto
baseline. If a candidate passes all three context gates (day/night/ir), install it formally.
