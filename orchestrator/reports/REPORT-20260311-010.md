# REPORT: TASK-20260311-010-primary-vs-primary-score-test

## Summary
Добавлены 2 теста конкуренции двух primary-целей в `TestSelectActivePolicy` в
`tests/test_target_manager_lock_policy.py`. Закрыт пробел, выявленный в REPORT-20260311-004.

## Changes
| Файл | Действие |
|---|---|
| `tests/test_target_manager_lock_policy.py` | Добавлены 2 теста в `TestSelectActivePolicy` |

## Добавленные тесты
1. `test_select_active_primary_vs_primary_higher_score_wins` — при `active_id=None` и двух primary
   с разным `drone_score` побеждает target с `drone_score=0.75` над `drone_score=0.50`.
   Проверяет `primary_score` формулу (3.2 * drone_score компонент).

2. `test_select_active_primary_vs_primary_speed_breaks_tie` — при равном `drone_score` и разном `speed`
   побеждает более быстрый target (speed=1.5 vs 0). Проверяет speed-компонент primary_score.

## Validation
```
Command: PYTHONPATH=src ./tracker_env/bin/python -m unittest -v tests.test_target_manager_lock_policy
Result:  Ran 14 tests in 0.001s — OK (все 14 зелёные)
```

## Residual Risks
- Тесты зависят от конкретных констант `primary_score` формулы. Если коэффициенты изменятся в
  `target_manager.py`, score-расчёт в docstring теста потребует пересчёта.

## Варианты эволюции
- Добавить тест на Phase-2 (два primary, один уже активен с drone_score меньше конкурента —
  проверить что Phase-1 early return действительно удерживает текущий активный).
