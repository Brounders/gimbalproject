# REPORT: TASK-20260312-056 — gui-display-card-extract

Task ID: TASK-20260312-056
Date: 2026-03-12
Status: Done

## Что сделано

1. Создан `app/ui/cards.py`:
   - Module docstring с пояснением источника и назначения
   - Imports: `QFrame`, `QGridLayout`, `QLabel`, `QVBoxLayout`
   - `build_inspector_card(title: str) -> tuple[QFrame, QLabel]` — pure factory (перенесён из `_build_inspector_card`)
   - `build_target_info_card() -> tuple[QFrame, QLabel, QLabel, QLabel, QLabel, QLabel]` — pure factory (перенесён из `_build_target_info_card`); возвращает card + 5 labels

2. Обновлён `app/main_gui.py`:
   - Добавлен import: `from app.ui.cards import build_inspector_card, build_target_info_card`
   - Удалён метод `_build_inspector_card` (~14 строк)
   - Удалён метод `_build_target_info_card` (~32 строки, включая inline `from PySide6.QtWidgets import QGridLayout`)
   - В `build_video_stage`: `self.target_info_card = self._build_target_info_card()` → распаковка 6-tuple из `build_target_info_card()`
   - В `build_inspector_drawer`: 5 вызовов `self._build_inspector_card(...)` → `build_inspector_card(...)`

## Что проверено

- `python3 -m compileall -q python_scripts src app orchestrator tests` — OK
- `PYTHONPATH=src python -m unittest -q tests.test_target_manager_lock_policy tests.test_package_import` — 15 тестов OK
- GUI smoke `MainWindow` — OK
- `main_tracker.py --help` — OK

## Риски

- Нет: обе функции — чистые фабрики без self-зависимостей. Caller распаковывает результаты в self.* атрибуты, семантика не изменилась.

## Что осталось

- Ничего. TASK закрыт.
