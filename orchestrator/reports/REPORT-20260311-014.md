# REPORT: TASK-20260311-014

Task ID: TASK-20260311-014
Owner: Claude Mac
Status: Completed
Date: 2026-03-11

## Что сделано

Создан скрипт `python_scripts/run_quick_kpi_smoke.py`.

**Возможности:**
- `--sources` (запятая) или `--pack` CSV (тот же формат, что `regression_pack.csv`)
- `--preset` (default/night/...), `--max-frames` (умолчание 300), `--mode`, `--device`
- `--output-json` и `--output-csv` для сохранения результатов
- Выходит KPI: `avg_fps`, `active_id_changes_per_min`, `lock_switches_per_min`, `false_lock_rate`, `continuity_score`, `active_presence_rate`
- Aggregate строка по всем источникам
- Exit code: 0 = OK, 1 = есть ошибки источников, 2 = аргументы/конфиг

## Какие файлы изменены

- `python_scripts/run_quick_kpi_smoke.py` (новый, ~200 LOC)

## Результаты валидации

```
python3 -m compileall -q python_scripts src app orchestrator  → OK (чисто)
python3 python_scripts/run_quick_kpi_smoke.py --help          → OK (помощь выводится)
```

## Риски

- `evaluate_source` запускает полный pipeline — может быть медленным на больших видео без `--max-frames`
- Нет GPU/CPU fallback-проверки — унаследовано из pipeline

## Что осталось

- Обновить `python_scripts/README.md` (если существует)
- Первый smoke-прогон на реальном клипе (ручная верификация вывода)
