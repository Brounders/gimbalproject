# AP-027 — Tasks 1, 2, 4, 5 Complete

**Date:** 2026-05-08  
**Branch:** adoring-williamson-17634e  

## Выполнено

### Task 1 — Структурированный результат сравнения (DTS)
- Добавлен метод `_parse_compare_structured()` в `DtsBridge` — парсит `summary.csv` в структурированный dict
- Добавлен метод `_apply_compare_structured()` — применяет данные к instance variables
- Новые `@Property`: `compareStatus` (NOT_RUN/RUNNING/PASS/FAIL/RETUNE), `compareBaselinePassN/TotalN`, `compareCandidatePassN/TotalN`, `compareContextsJson`
- `_load_latest_compare_summary()` и `_on_compare_finished()` теперь вызывают `_apply_compare_structured()`
- В `DtsOverlay.qml` блок КАНДИДАТ заменён: status badge, base N/M, cand N/M, per-context строки (pres/Δpres/fl/id), путь к директории результата

### Task 2 — Реальный прогресс сравнения
- `run_model_battle.py`: добавлены PROGRESS-строки перед каждым `_run_gate()` вызовом — `PROGRESS context=X step=N total=M`
- `DtsBridge`: подключён `readyReadStandardOutput` в `compareCandidate()`; добавлен `_on_compare_stdout()` для парсинга PROGRESS-строк
- Новые `@Property`: `compareStep`, `compareTotal`, `compareContextLabel`  
- В DtsOverlay: индетерминированная анимация заменена сегментированным прогресс-баром с шагом и контекстом; Behavior on width для плавности

### Task 3 — Аудит настроек
- Все 14 tracker-настроек уже подключены через `_apply_tracker_settings()` → `Config`
- `tracker_operator_annotation_enabled` подключён (строка 256 tracker_bridge.py)
- СИСТЕМА/КАМЕРА/СЕТЬ вкладки Settings — статические стабы; не блокируют функциональность

### Task 4 — Click-to-lock фидбек
- `selectTargetAtFrame()`: сообщение изменено на `"ручное указание цели ({x},{y}) — поиск вокруг клика..."` (было: "Выбор цели по клику: x=Y y=Z")
- Существующий `trackingState` property показывает переход в LOCK_FOCUS — дополнительной логики не требуется

### Task 5 — QML cleanup
- Production TopBar не содержит debug-табов (DTS/ЭКСПЕРТ/КАЛИБРОВКА/ДИАГНОСТИКА/НАСТРОЙКИ/О СИСТЕМЕ)
- VideoSurface: z=0, fullscreen — уже является визуальным центром
- DtsOverlay: КАНДИДАТ блок переведён на `implicitHeight` вместо фиксированных 254px

## Проверено
- `python3 -m compileall -q app/ python_scripts/run_model_battle.py` → OK
- `python3 -m unittest discover -s tests -q` → OK (15/15)
- Braces balance в DtsOverlay.qml → 149/149

## Риски
- Segmented progress bar потребует PROGRESS-строк в stdout; старые runs без них покажут 0/0 (fallback: индикатор не виден когда compareRunning=false)
- `_on_compare_stdout` читает `readAllStandardOutput()` — если battle script печатает не-PROGRESS строки вперемешку, они тихо игнорируются (ок)
- Высота КАНДИДАТ блока теперь динамическая (`implicitHeight`) — при 0 context rows блок показывает минимальную высоту 160px

## Файлы
- `python_scripts/run_model_battle.py` — PROGRESS prints
- `app/qml_bridge/dts_bridge.py` — structured properties + stdout handler
- `app/qml_bridge/tracker_bridge.py` — click message
- `app/qml/components/DtsOverlay.qml` — structured panel + segmented progress
