# REPORT: TASK-20260312-064 — noise-scene lock gating

Task ID: TASK-20260312-064
Date: 2026-03-12
Status: Done

## Что сделано

1. Обновлён `src/uav_tracker/profile_io.py`:
   - В dict `mapping` после `lock_confirm_frames` добавлена запись:
     `'lock_lost_grace': 'LOCK_LOST_GRACE'`
   - Теперь YAML-ключ `lock_lost_grace` маппируется на `Config.LOCK_LOST_GRACE`

2. Обновлён `configs/night.yaml`:
   - Добавлено: `lock_lost_grace: 1  # [night hardening] reduced from default 2`

3. Обновлён `configs/antiuav_thermal.yaml`:
   - Добавлено: `lock_lost_grace: 1  # [ir hardening] reduced from default 2`

## Что проверено

- `python3 -m compileall -q python_scripts src app orchestrator tests` — OK
- `load_preset('night').LOCK_LOST_GRACE == 1` ✅
- `load_preset('antiuav_thermal').LOCK_LOST_GRACE == 1` ✅
- 15 unit tests — OK

## Риски

- Снижение `lock_lost_grace` с 2 до 1 уменьшает tolerance к 1-кадровым detection gaps.
  Может незначительно снизить `lock_rate` при нестабильной детекции быстрых целей.
- Pipeline/tracking logic не затронуты.

## Что осталось

- Ничего. TASK закрыт.
