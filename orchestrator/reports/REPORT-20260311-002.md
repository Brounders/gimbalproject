# REPORT: TASK-20260311-002-lock-policy-unit-tests

## Summary
Создан минимальный тестовый контур для lock/ID-policy TargetManager. Добавлено 8 детерминированных unit-тестов без внешних зависимостей, без изменения runtime-кода. Тесты запускаются одной командой и проходят все 8.

Покрытые сценарии:
1. **Lock confirmation hysteresis** — has_confirmed_drone_lock: 3 теста (insufficient streak, all criteria met, lost_frames > grace).
2. **Active ID switch policy** — 2 теста (blocked in strict focus mode, allowed after cooldown expires).
3. **LOST transition** — 1 тест (primary target removed + active_id cleared after YOLO_LOST_MAX frames).
4. **Focus mode hysteresis** — 2 теста (enter only after ACQUIRE_FRAMES, exit only after RELEASE_FRAMES).

## Changes
- File: `tests/__init__.py` — создан (пустой package init)
- File: `tests/test_target_manager_lock_policy.py` — создан, 8 test methods в 4 test classes
- File: `tests/README.md` — создан, инструкция по запуску

## Validation
- Command: `PYTHONPATH=src python3 -m compileall -q python_scripts src app tests`
- Result: OK, ошибок нет
- Command: `PYTHONPATH=src python3 -m unittest -v tests.test_target_manager_lock_policy`
- Result: Ran 8 tests in 0.000s — OK

```
test_id_switch_allowed_after_cooldown_expires ... ok
test_id_switch_blocked_in_strict_focus_mode ... ok
test_focus_mode_enters_only_after_acquire_frames ... ok
test_focus_mode_exits_only_after_release_frames ... ok
test_lock_confirmed_when_all_criteria_met ... ok
test_lock_not_confirmed_when_lost_frames_exceed_grace ... ok
test_lock_not_confirmed_with_insufficient_streak ... ok
test_primary_target_removed_after_yolo_lost_max ... ok
```

## Residual Risks
- Risk: тесты используют прямой inject в `manager.targets` и выставление `_focus_mode` напрямую. Если внутренняя структура TargetManager изменится, тесты потребуют обновления.
- Mitigation: тесты проверяют только публичные методы (`has_confirmed_drone_lock`, `is_focus_mode`, `age_targets`, `update_focus_mode`, `_set_active_id`). Inject через `_inject()` — единственное место, которое завязано на внутренний `TrackedTarget`.

## Next Step
- Proposed follow-up: добавить тест для `select_active()` при наличии нескольких конкурирующих целей (primary vs aux).
