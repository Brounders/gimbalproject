# REPORT: TASK-20260312-057 — gui-nonworker-plumbing-thinning

Task ID: TASK-20260312-057
Date: 2026-03-12
Status: Done

## Что сделано

После TASK-055 и TASK-056 `app/main_gui.py` уменьшился с 1660 до 1598 строк (−62 строки).

Проверено содержимое `app/main_gui.py`:
- Imports актуальны: `QGridLayout`, `QVBoxLayout`, `QFrame`, `QWidget` — все используются в оставшемся коде
- Нет мёртвых imports и хвостовых блоков
- `build_inspector_drawer` остаётся методом `MainWindow` (имеет self-зависимости + нестандартный `events_card` блок)
- `CANONICAL_OPERATOR_MODES` остаётся в `main_gui.py` (бизнес-логика, не стиль)
- `app/ui/cards.py` и `app/ui/theme.py` компилируются чисто

## Что проверено

- `wc -l app/main_gui.py` → 1598 строк (было 1660, −62)
- `python3 -m compileall -q python_scripts src app orchestrator tests` — OK
- `PYTHONPATH=src python -m unittest -q tests.test_target_manager_lock_policy tests.test_package_import` — 15 тестов OK
- GUI smoke `MainWindow` — OK
- `main_tracker.py --help` — OK

## Риски

- Нет: строго структурная операция без изменения runtime-семантики.

## Что осталось

- Ничего. TASK закрыт.
