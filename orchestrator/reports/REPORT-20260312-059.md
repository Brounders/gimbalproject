# REPORT: TASK-20260312-059 — baseline-candidate-decision-hardening

Task ID: TASK-20260312-059
Date: 2026-03-12
Status: Done

## Что сделано

1. Обновлён `python_scripts/install_baseline.py`:
   - Добавлена функция `_parse_preset_gates(spec: str) -> dict[str, str]` — парсит `"day=path1,night=path2,ir=path3"` → `{"day": ..., ...}`
   - Добавлен `--preset-gates` аргумент в `parse_args()` (comma-separated key=path pairs)
   - В manifest добавлено поле `preset_gate_reports: _parse_preset_gates(args.preset_gates)` — пустой dict если не указан

2. Обновлён `RUNBOOK.md`:
   - Секция "Локальный quality-gate flow": Шаг 3 заменён на явный per-context flow (3a day + 3b night + 3c ir)
   - Добавлено явное правило promote: ВСЕ три context-gate должны вернуть exit 0
   - Секция "Decision artifact standard": добавлено `--preset-gates` в canonical `install_baseline.py` invoke
   - Добавлена canonical output таблица (три context → три JSON пути)

3. Обновлён `models/README.md`:
   - State transition rules: `quality gate PASS` → `ВСЕ три context-gate PASS (day + night + ir)`
   - Добавлены канонические command examples для трёх context-gate
   - `baseline_manifest.json` format обновлён: добавлено поле `preset_gate_reports`

## Что проверено

- `python3 -m compileall -q python_scripts` — OK
- `install_baseline.py --help` — `--preset-gates` присутствует
- 15 unit tests — OK

## Риски

- Нет: `--preset-gates` — optional arg, manifest обратно совместим (поле `{}` если не указан).

## Что осталось

- Ничего. TASK закрыт.
