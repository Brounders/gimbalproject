# REPORT: TASK-20260312-061 — night-ir-false-lock-suppression

Task ID: TASK-20260312-061
Date: 2026-03-12
Status: Done

## Что сделано

1. Обновлён `src/uav_tracker/profile_io.py`:
   - В dict `mapping` функции `apply_overrides` добавлена запись после `velocity_alpha`:
     `'lock_confirm_frames': 'LOCK_CONFIRM_FRAMES'`
   - Теперь YAML-ключ `lock_confirm_frames` маппируется на `Config.LOCK_CONFIRM_FRAMES`

2. Обновлён `configs/night.yaml`:
   - `drone_lock_score_min: 0.58` → `0.64` (более строгий порог lock) `# [night hardening]`
   - `drone_reacquire_score_min: 0.45` → `0.52` (строже reacquire) `# [night hardening]`
   - Добавлено: `lock_confirm_frames: 7` (было default 5) `# [night hardening]`

3. Обновлён `configs/antiuav_thermal.yaml`:
   - `drone_lock_score_min: 0.56` → `0.60` `# [ir hardening]`
   - `drone_reacquire_score_min: 0.42` → `0.48` (алайн с default) `# [ir hardening]`
   - Добавлено: `lock_confirm_frames: 7` (было default 5) `# [ir hardening]`

## Что проверено

- `python3 -m compileall -q python_scripts src app orchestrator tests` — OK
- PYTHONPATH=src: `load_preset('night')` → `cfg.LOCK_CONFIRM_FRAMES == 7` ✅
- PYTHONPATH=src: `load_preset('antiuav_thermal')` → `cfg.LOCK_CONFIRM_FRAMES == 7` ✅
- PYTHONPATH=src: `load_preset('night')` → `cfg.DRONE_LOCK_SCORE_MIN == 0.64` ✅
- PYTHONPATH=src: `load_preset('antiuav_thermal')` → `cfg.DRONE_LOCK_SCORE_MIN == 0.60` ✅
- 15 unit tests — OK

## Риски

- Нет: только YAML значения + один mapping entry. Pipeline/tracking logic не затронуты.
- `lock_confirm_frames: 7` — между default(5) и `AUTO_SCENE_NIGHT_LOCK_CONFIRM`(8); безопасный диапазон.
- Повышение `drone_lock_score_min` может снизить `lock_rate` на сложных ночных клипах — это ожидаемый trade-off.

## Что осталось

- Ничего. TASK закрыт.
