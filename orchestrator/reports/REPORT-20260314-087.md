# REPORT: REPORT-20260314-087 — Tech Debt Closure (AP-20260314-027)

Date: 2026-03-14
Branch: agent-team/2026-03-14-quick-wins
Status: Done — все запланированные фазы выполнены

---

## Что сделано

### Phase 1 — Стабилизация
- **A02** (уже): print() → logging в run_tracker (коммит 87fc939)
- **A05** (уже): _iou() → utils/geometry.py (коммит 4fa10d1)
- **A11** (уже): try/except в UltralyticsBackend (коммит dcd9483)
- **baseline.pt**: уже установлен (drone_bird_probe_fast, 2026-03-14)
- **A01**: `_stop_requested`/`_switch_target_requested` → `threading.Event` в TrackerWorker и EvaluationWorker
- **A03**: overlay import перенесён из строки 967 в начало pipeline.py
- **A12**: NIGHT_MOG2_HISTORY=50, NIGHT_MOG2_VAR_THRESH=25, NIGHT_BLUR_KERNEL=5, NIGHT_MORPH_KERNEL=3, NIGHT_GRID_CELL=8 добавлены в Config; night_detector.py обновлён

### Phase 2 — Day Gate Fix
- `run_quality_gate.py`: `false_lock_rate` check пропускается когда `gt_frames=0`
- `_score_row()`: не штрафует за false_lock без GT
- `gt_frames` добавлен в row dict и CSV-заголовки

### Phase 3 — Документация (A06)
- Класс-docstring `TrackerPipeline` (7 компонентов + usage example)
- Метод-docstring `process_frame` (Args/Returns с типами и семантикой)

### Phase 4 — Архитектура
- **A09**: `config.py` структурирован по 10 логическим секциям (без breaking API change)
- **BRIEF-030**: Config nested groups (вариант с breaking API — на будущую сессию)
- **BRIEF-031**: TrackerPipeline decomposition (A08, после тестов)

### Phase 5 — Алгоритмика (A07)
- Kalman vs EMA: решение ОСТАВИТЬ EMA, зафиксировано в BRIEF-032
- Обоснование: все провалы gate были на уровне детектора, не velocity prediction

### Phase 6 — Training стратегия
- Корневая причина: drone-bird-yolo не содержит night visible-light данных
- BRIEF-033: шаги до следующего обучения (аудит датасета → новый датасет → early stopping)

---

## Что проверено

- `python3 -m compileall -q src app python_scripts` — OK
- `python3 -m unittest discover -s tests` — 15/15 OK
- Smoke-import: pipeline, night_detector, ultralytics_backend, geometry.iou — OK
- Config fields all present, Night(50), Lock(5), AutoScene(False)

---

## Что осталось (не в этом plan)

| ID | Описание | Brief |
|----|----------|-------|
| A08 | TrackerPipeline decomposition | BRIEF-031 |
| A09* | Config nested groups (breaking) | BRIEF-030 |
| A10 | Тесты >30% | Условный |
| Training | Аудит датасета + новый brief | BRIEF-033 |

*A09 безопасный вариант (секции) выполнен в этом plan.

---

## Риски

- A08 без тестового покрытия — высокий риск. Рекомендуется сначала A10.
- Day gate false_lock теперь не проверяется без GT. Для полноценного gate нужен GT-файл для дневного клипа.
- threading.Event в TrackerWorker не тестируется юнит-тестами (GUI-код).
