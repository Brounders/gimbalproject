# REPORT: TASK-20260313-074 — noise vs large night separation

Task ID: TASK-20260313-074
Date: 2026-03-13
Status: Done

## Что сделано

1. Обновлён `python_scripts/run_quality_gate.py`:
   - Добавлен аргумент `--max-night-id-changes-per-min` (type: float, default: 8.0)
   - Gate logic расширен: `is_night = scene == "night"` — night scenes теперь используют
     отдельный threshold `max_night_id_changes_per_min` (failure tag: `night_idchg/min>`)
   - Три независимые ветки: `noise/noisy/background` → noise gate; `night` → night gate; остальные → general gate
   - `thresholds` dict в JSON output дополнен полем `max_night_id_changes_per_min`

2. Обновлён `configs/problem_pack_gate_contract.json`:
   - Добавлено в `thresholds`: `"max_night_id_changes_per_min": 18.0`
   - Добавлено в `rationale`: объяснение 18 vs default 8 (explicit night separation)
   - Обновлены `canonical_invocations["night"]`: добавлен `--max-night-id-changes-per-min 18`

3. Обновлён `python_scripts/run_problem_pack_gate.py`:
   - В `threshold_args` добавлен: `"--max-night-id-changes-per-min", str(t["max_night_id_changes_per_min"])`

## Что проверено

- `python3 -m compileall -q python_scripts src app orchestrator tests` — OK
- `run_quality_gate.py --help | grep night-id` — аргумент присутствует ✅
- `problem_pack_gate_contract.json["thresholds"]["max_night_id_changes_per_min"] == 18.0` ✅
- 15 unit tests — OK

## Риски

- Изменение additive: новый arg + новая ветка в scene-classification block.
  Существующие noise и non-night/non-noise логики не изменены.
- Default 8.0 для `--max-night-id-changes-per-min` совпадает с `--max-id-changes-per-min` —
  бэксовместимость сохранена (поведение не меняется без явного флага).

## Что осталось

- Ничего. TASK закрыт.
