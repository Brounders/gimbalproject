# REPORT: TASK-20260311-009-safe-reorg-plan

## Summary
Создан безопасный поэтапный план реорганизации файловой системы проекта.
Артефакт: `orchestrator/state/filesystem_reorg_plan.md`. Никаких перемещений/удалений в рамках задачи.

## Changes
| Файл | Действие |
|---|---|
| `orchestrator/state/filesystem_reorg_plan.md` | Создан — план из 3 фаз с dry-run командами |

## Структура плана

### Phase 1 — Только аудит (безопасно, без деструктивных операций)
- 1.1: Верифицировать, что root-wrappers не импортируются из активного кода
- 1.2: Убедиться что `legacy/prototypes/` содержит оригиналы
- 1.3: Верифицировать safety_snapshots актуальность

### Phase 2 — Минимальная очистка root (только после P1 + Human approve)
- 2.1: `git rm` пяти root-wrapper .py файлов + коммит с валидацией
- 2.2: Удалить пустую директорию `arduino_sketches/`
- 2.3: Опционально — архивировать `safety_snapshots/` в tarball

### Phase 3 — Управление artifacts (только после P2 + Human approve)
- 3.1: Очистка старых RTX unpacked artifacts
- 3.2: Ротация логов (gzip для старше 30 дней)

## Раздел "Запрещено трогать"
- `datasets/` — никаких операций без явного Human-approve + backup verify
- `runs/` — хранить все; очистка через workflow
- `tracker_env/` — Python environment, нельзя трогать
- `src/uav_tracker/`, `app/` — бизнес-логика
- `main_tracker.py`, `tracker_gui.py` — runtime entrypoints
- `models/checkpoints/rtx_latest_best.pt` — текущий baseline

## Validation
- План содержит dry-run команды для каждого шага Phase 1 и Phase 2.
- Нет команд удаления в Phase 1.
- Для каждого шага Phase 2 есть: команда + rollback + чек-лист.
- compileall: OK.

## Residual Risks
- Phase 2 и Phase 3 — только после явного Human approve. Данная задача не выполняет их.
- root-wrapper файлы (`benchmark.py` и др.) содержат только compatibility headers,
  но удаление требует верификации P1.1 перед выполнением.

## Следующий шаг (опционально)
- При желании выполнить Phase 1 (только аудит) можно немедленно — она безопасна.
- Phase 2 — только после Human approve.
