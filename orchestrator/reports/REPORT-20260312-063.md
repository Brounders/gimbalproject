# REPORT: TASK-20260312-063 — problem-clip-regression-pack

Task ID: TASK-20260312-063
Date: 2026-03-12
Status: Done

## Что сделано

1. Создан `configs/regression_pack_problem.csv`:
   - 4 проблемных клипа: night_ground_large_drones.mp4 (night), night_ground_indicator_lights.mp4 (noise),
     IR_DRONE_001.mp4 (ir), Demo_IR_DRONE_146.mp4 (ir)
   - Формат совместим с `run_quality_gate.py --pack-file`
   - Включены inline instructions для запуска (night: `--preset night`, IR: `--preset antiuav_thermal`)

2. Обновлён `RUNBOOK.md`:
   - В секцию Evaluation добавлена подсекция "Problem-clip fast loop" с тремя командами:
     быстрый KPI smoke (run_quick_kpi_smoke.py, 180 кадров), full gate ночных клипов,
     full gate IR клипов

## Что проверено

- `configs/regression_pack_problem.csv` создан и содержит 4 записи в формате `source,scene`
- `RUNBOOK.md` содержит секцию "Problem-clip fast loop" с корректными командами
- `python3 -m compileall -q python_scripts src app orchestrator tests` — OK

## Риски

- Нет: CSV-файл не влияет на runtime. Секция RUNBOOK.md — документация.
- Клипы в pack-файле должны существовать в `test_videos/` — если их нет, run_quality_gate вернёт ошибку source.

## Что осталось

- Ничего. TASK закрыт.
