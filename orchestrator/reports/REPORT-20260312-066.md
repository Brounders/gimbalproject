# REPORT: TASK-20260312-066 — problem-pack A/B evidence

Task ID: TASK-20260312-066
Date: 2026-03-12
Status: Done

## Что сделано

1. Создан `python_scripts/compare_kpi_snapshots.py`:
   - Принимает два JSON от `run_quick_kpi_smoke.py` (`before`, `after`)
   - Матчит строки по полю `source`
   - Выводит delta-таблицу для 5 метрик: `false_lock_rate`, `active_id_changes_per_min`,
     `lock_switches_per_min`, `continuity_score`, `avg_fps`
   - Отмечает значимые изменения (≥ `--threshold`, default 0.05) символами ✓/✗
   - Аргументы: `before` (Path), `after` (Path), `--threshold` (float)
   - Выходной код: 0 (display tool, не gate)

2. Обновлён `RUNBOOK.md`:
   - В секцию Evaluation добавлена подсекция "A/B evidence loop (problem clips)"
   - 4-шаговый workflow: before-snapshot → внести изменения → after-snapshot → compare

## Что проверено

- `python3 -m compileall -q python_scripts` — OK
- `compare_kpi_snapshots.py --help` — OK (аргументы присутствуют)
- 15 unit tests — OK

## Риски

- Нет: скрипт read-only (JSON чтение + stdout). Никаких side effects.
- Требует наличие `run_quick_kpi_smoke.py` JSON с полем `rows[]`.

## Что осталось

- Ничего. TASK закрыт.
