# REPORT: TASK-20260311-018

Task ID: TASK-20260311-018
Owner: Claude Mac
Status: Completed
Date: 2026-03-11

## Что сделано

Снижена визуальная дерготня state-отображения без изменения decision loop.

### pipeline.py

1. Добавлены поля `_display_tracking_state = 'SCAN'` и `_display_state_hold = 0` в `__init__`.

2. Добавлен метод `_get_display_tracking_state(real_state: str) -> str`:
   - Апгрейды (SCAN→TRACK) применяются мгновенно
   - Даунгрейды (TRACK→LOST, LOST→SCAN) удерживаются `DISPLAY_STATE_HOLD_FRAMES` кадров (default 3, конфигурируется через `cfg.DISPLAY_STATE_HOLD_FRAMES`)
   - Graceful fallback: если атрибут не задан в Config → 3 кадра

3. В `process_frame`: вычисляется `display_tracking_mode = _get_display_tracking_state(tracking_mode)`.
   - `draw_frame(tracking_mode=display_tracking_mode, ...)` — overlay показывает сглаженный режим
   - `FrameOutput(mode=tracking_mode, ...)` — метрики и UI badge получают реальный режим без изменений

### main_gui.py (`_update_stats`)

Добавлен 3-кадровый debounce перехода LOCK→RUNNING в UI badge:
```python
elif self._target_present_latched and self._target_missing_streak < 3:
    self._state_machine.set(UIState.LOCK)  # удерживаем LOCK при кратком SCAN
```
Это предотвращает мигание LOCK-badge при единичных пропусках кадра.

## Scope compliance

- TargetManager не изменён
- Lock-policy не изменена
- Decision loop не изменён
- Метрики (mode field, continuity_score и т.д.) не затронуты

## Какие файлы изменены

- `src/uav_tracker/pipeline.py` (2 поля + 1 метод + 1 строка в process_frame)
- `app/main_gui.py` (2 строки в _update_stats)

## Результаты валидации

```
python3 -m compileall -q python_scripts src app orchestrator  → OK
check_orchestration_state.py  → OK
```

## Риски

- `DISPLAY_STATE_HOLD_FRAMES=3` — небольшой лаг overlay. При быстрых TRACK/LOST переходах overlay может слегка отставать. Значение можно увеличить до 5-8 при необходимости.
- UI debounce (3 кадра) работает только для перехода SCAN-на-latch. Если TRACK→LOST→SCAN происходит подряд (> 3 кадра каждый), badge переключается корректно.

## Что осталось

- Визуальная проверка на реальном видео: убедиться что overlay-текст не мигает при потере цели на 1-2 кадра
