# REPORT: TASK-20260312-047 — preset-specific-regression-packs

Task ID: TASK-20260312-047
Date: 2026-03-12
Status: Done

## Что сделано

1. Созданы три сценарных pack-файла из `configs/regression_pack.csv`:

   | Файл | Пресет | Клипы |
   |------|--------|-------|
   | `configs/regression_pack_day.csv` | `default` | `drone_closeup_mixkit_44644_360.mp4` (day) |
   | `configs/regression_pack_night.csv` | `night` | `night_ground_large_drones.mp4` (night), `night_ground_indicator_lights.mp4` (noise) |
   | `configs/regression_pack_ir.csv` | `antiuav_thermal` | `Demo_IR_DRONE_146.mp4` (ir), `IR_DRONE_001.mp4` (ir), `IR_BIRD_001.mp4` (noise) |

   Формат: идентичен `regression_pack.csv` (comment header + `source,scene` строки).

2. Обновлён `RUNBOOK.md` — секция "Quality gate (regression)":
   - Добавлена таблица pack-файлов с пресетами
   - Добавлены примеры запуска `run_quality_gate.py --pack-file ... --preset ...`
   - Tooling уже поддерживал `--pack-file` — изменений в скриптах не потребовалось
   - `run_offline_benchmark.py` использует аналогичный `--source-list` аргумент (тот же CSV формат)

## Что проверено

- `PYTHONPATH=src ./tracker_env/bin/python python_scripts/run_quality_gate.py --help` — `--pack-file` аргумент присутствует.
- `PYTHONPATH=src ./tracker_env/bin/python python_scripts/run_offline_benchmark.py --help` — `--source-list` аргумент присутствует.
- `python3 -m compileall -q` — чисто.

## Риски

- Нет: pack-файлы — только CSV конфигурация, нет изменений в runtime.

## Что осталось

- Ничего. TASK закрыт.
