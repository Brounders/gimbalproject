# REPORT: TASK-20260312-067 — night-noise scene classification gating

Task ID: TASK-20260312-067
Date: 2026-03-12
Status: Done

## Что сделано

Обновлён `python_scripts/run_quality_gate.py`:

1. Добавлен новый аргумент в `parse_args()`:
   `--max-noise-id-changes-per-min` (type: float, default: 15.0)

2. Изменена логика gating (строки ~263-267):
   - Noise сцены (`noise`, `noisy`, `background`) теперь используют
     `noise_idchg/min > max_noise_id_changes_per_min` (default 15.0)
   - Non-noise сцены используют прежний `idchg/min > max_id_changes_per_min` (default 8.0)
   - Noise и non-noise клипы теперь полностью разделены по ID-change threshold

3. Обновлён `thresholds` dict в JSON output:
   добавлено поле `"max_noise_id_changes_per_min"`.

## Что проверено

- `python3 -m compileall -q python_scripts src` — OK
- `run_quality_gate.py --help | grep noise-id` — аргумент присутствует
- 15 unit tests — OK

## Риски

- Изменение additive: новый arg + новая ветка в is_noise block.
  Существующая non-noise логика не затронута.
- Default 15.0 для noise более lenient, чем 8.0 для night scenes, что соответствует
  природе noise clips (нет реальной цели → выше ID instability структурно).

## Что осталось

- Ничего. TASK закрыт.
