# REPORT: TASK-20260313-078 — Night Detector Profile Contract

Task ID: TASK-20260313-078
Date: 2026-03-13
Status: Done
Type: Code + Documentation. No training, no GUI changes, no runtime-wide rewrite.

---

## Что сделано

### 1. `src/uav_tracker/profile_io.py` — расширен mapping `apply_overrides()`

Добавлены три новых entry в mapping dict (перед `night_run_when_primary_seen`):

```python
'night_max_area': 'NIGHT_MAX_AREA',
'night_track_dist': 'NIGHT_TRACK_DIST',
'night_lost_max': 'NIGHT_LOST_MAX',
```

До AP-024 эти три параметра не были доступны через YAML contract.
Все остальные entries и логика `apply_overrides()` не затронуты.

### 2. `OPERATOR_BASELINE.md` — обновлена документация

- Добавлена секция **Night Detector Knobs (AP-024)** с таблицей трёх новых knob-ов,
  их default-значениями и назначением.
- Обновлена таблица **Preset Runtime Tuning Contract** — добавлены три строки
  `night_max_area`, `night_track_dist`, `night_lost_max` с текущими значениями для `night` пресета.

---

## Что проверено

- `python3 -m compileall -q python_scripts src app` — **PASS** (нет ошибок компиляции)
- pytest/unittest — **No pytest suite configured** (ожидаемо)
- Чтение `profile_io.py` после правки: три новых строки на месте, структура mapping не нарушена

---

## Контракт после правки

| YAML ключ | Config поле | Default | Готов к тюнингу |
|-----------|------------|---------|----------------|
| `night_max_area` | `NIGHT_MAX_AREA` | 200 | ✅ |
| `night_track_dist` | `NIGHT_TRACK_DIST` | 42 | ✅ |
| `night_lost_max` | `NIGHT_LOST_MAX` | 8 | ✅ |

---

## Риски

- Минимальные. Новые YAML ключи игнорируются если отсутствуют в preset файле
  (logic: `attr = mapping.get(key)` → `if attr is None: continue`).
- Без изменений в `night.yaml` behaviour идентичен pre-AP-024.
- Регрессия на других preset'ах исключена: mapping расширен, не изменён.

---

## Что осталось

- TASK-079: применить значения в `configs/night.yaml` для large-target night tuning
- TASK-080: evidence loop + before/after verdict
