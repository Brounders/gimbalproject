# REPORT: TASK-20260312-051 — quality-decision-artifact-standard

Task ID: TASK-20260312-051
Date: 2026-03-12
Status: Done

## Что сделано

1. Прочитан реальный gate output JSON (`runs/evaluations/quality_gate/curriculum_chunk1_20260312_recheckquality_gate_night.json`) для понимания реальной структуры.

2. Добавлена подсекция "Decision artifact standard" в RUNBOOK.md секцию "Локальный quality-gate flow":
   - Canonical output path: `runs/evaluations/quality_gate/<tag>_quality_gate_<preset>.json`
   - Tag convention: `--tag baseline_YYYYMMDD` / `--tag candidate_vN_YYYYMMDD`
   - Таблица ключевых полей JSON: `gate_passed`, `failures`, `mean`, `rows[].passed`, `rows[].fail_reasons`
   - Правило принятия решения:
     - `gate_passed = true` → accept → install via `install_baseline.py --gate-report <json>`
     - `gate_passed = false`, tunable → `hold_and_tune`
     - `gate_passed = false`, регресс → `reject`
   - Ссылка на `models/README.md` как governance contract

3. Без изменений в скриптах — существующие gate outputs полностью покрывают контракт.

## Что проверено

- `PYTHONPATH=src ./tracker_env/bin/python python_scripts/run_quality_gate.py --help` — работает.
- `python3 -m compileall -q` — чисто.
- Реальный gate JSON содержит все задокументированные поля.

## Риски

- Нет: только документация.

## Что осталось

- Ничего. TASK закрыт.
