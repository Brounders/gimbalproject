# REPORT: TASK-20260312-055 — gui-theme-style-extract

Task ID: TASK-20260312-055
Date: 2026-03-12
Status: Done

## Что сделано

1. Обновлён `app/ui/theme.py`:
   - Добавлен import `from PySide6.QtWidgets import QWidget`
   - Перенесён `SCENARIO_LABELS: dict[str, str]` из `app/main_gui.py` (7 строк)
   - Добавлена функция `refresh_widget_style(widget: QWidget) -> None` на основе метода `_refresh_widget_style` из `MainWindow` (6 строк)
   - Обновлён docstring — добавлены `SCENARIO_LABELS`, `refresh_widget_style` в import-пример

2. Обновлён `app/main_gui.py`:
   - Обновлён import: `from app.ui.theme import APP_STYLESHEET, SCENARIO_LABELS, refresh_widget_style`
   - Удалён блок `SCENARIO_LABELS` (7 строк)
   - Удалён метод `_refresh_widget_style` (6 строк)
   - 6 call sites `self._refresh_widget_style(w)` → `refresh_widget_style(w)`

## Что проверено

- `python3 -m compileall -q python_scripts src app orchestrator tests` — OK
- `PYTHONPATH=src python -m unittest -q tests.test_target_manager_lock_policy tests.test_package_import` — 15 тестов OK
- GUI smoke `MainWindow` — OK

## Риски

- Нет: `SCENARIO_LABELS` — чистое перемещение словаря без логики. `refresh_widget_style` — чистое перемещение утилиты без self-зависимостей.

## Что осталось

- Ничего. TASK закрыт.
