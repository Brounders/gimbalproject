# REPORT: TASK-20260312-060 — quality-report-normalization

Task ID: TASK-20260312-060
Date: 2026-03-12
Status: Done

## Что сделано

1. Обновлён `python_scripts/run_quality_gate.py`:
   - В JSON summary добавлено поле `report_type: "quality_gate"` (уже включено в TASK-058)
   - В JSON summary добавлено поле `context` (уже включено в TASK-058)

2. Обновлён `python_scripts/run_offline_benchmark.py`:
   - В JSON output добавлены поля: `report_type: "benchmark"`, `tag: args.tag`
   - Это позволяет программно различать gate и benchmark JSON без анализа имени файла

3. Обновлён `RUNBOOK.md`:
   - Добавлена секция "Canonical summary contract" (сравнительная таблица gate vs benchmark)
   - Правило: `quality_gate_<preset>.json` = canonical decision artifact; benchmark = exploratory

## Что проверено

- `python3 -m compileall -q python_scripts` — OK
- `run_offline_benchmark.py --help` — OK
- `run_quality_gate.py --help` — OK
- 15 unit tests — OK

## Риски

- Нет: `report_type` и `tag` — новые поля в JSON output без изменения существующих полей или логики.

## Что осталось

- Ничего. TASK закрыт.
