# GimbalProject — Production runtime dependencies (Mac M1 desktop)
#
# Setup:
#   python3 -m venv tracker_env
#   source tracker_env/bin/activate
#   pip install -r requirements.txt
#
# Note: training-specific dependencies (wandb, etc.) are NOT required
# for local desktop runtime or evaluation.

# Core runtime — YOLO + tracking
torch>=2.0
torchvision>=0.15
ultralytics>=8.0
opencv-python>=4.8
numpy>=1.24
PyYAML>=6.0
lapx>=0.5
scipy>=1.10
tqdm>=4.65
pillow>=10.0

# GUI (Mac desktop)
PySide6>=6.5

# Evaluation / quality-gate / automation intake
requests>=2.28
matplotlib>=3.7
